# OneDrive 持久缓存与 SPlayer 功能恢复计划

## 已确认的问题

- 当前搜索、歌词和播放地址只缓存在服务进程内，重启或容量淘汰后会丢失。
- 搜索结果中的 `pic_id` 被后端丢弃，前端把封面固定成默认唱片图，因此浏览器从未请求真实封面。
- 现有改造把首页、发现音乐、播客电台、收藏、歌单和搜索建议等原始入口直接隐藏或删除，偏离 SPlayer demo。

## OneDrive 实验结论

- 匿名编辑分享令牌可以创建文件，并能按文件 ID 原样读回。
- 写入接口支持固定路径；正式实现会补充同名覆盖测试，用覆盖替代删除。
- 匿名令牌不能删除文件或移入回收站，因此不能把 OneDrive 当作支持任意淘汰的完整对象存储。

## 拟实施方案

### 1. 三层缓存

- L1：进程内存，负责热数据与请求合并。
- L2：OneDrive，按稳定哈希文件名持久保存搜索、歌词和封面解析结果。
- L3：GD 音乐台，仅在 L1/L2 均未命中或数据过期时访问。
- 缓存文件内部记录版本、创建时间、过期时间和数据；过期文件采用同名覆盖，不依赖删除。
- 允许短时间提供过期数据，并由单个后台请求刷新，避免并发访问同时冲击 GDAPI。

### 2. 封面链路

- 后端保留搜索结果的 `pic_id`。
- 新增封面解析接口，优先从 L1/L2 读取真实图片 URL，未命中时请求 GD 的 `types=pic` 后写入 OneDrive。
- 浏览器最终直接请求图片源站，不由游戏云代理图片字节。
- 列表、底部播放器和全屏歌词页统一使用真实封面；只有图片加载失败时才回退默认唱片图。

### 3. SPlayer 外壳恢复

- 以改造前的原版组件为基线，恢复 `HomeOnline` / `HomeLocal` 首页结构和原始侧栏显隐逻辑。
- 恢复搜索框的默认内容与建议组件，并保留 GD 全网歌曲搜索作为当前可用的数据能力。
- 不再用 `show: false` 粗暴隐藏原有入口。
- 暂无可用数据 API 的页面保留 SPlayer 原结构，但不会伪造数据；后续接入用户提供的其他 API。
- 保留 2moro 品牌、GD 来源标注、GD 播放与歌词适配。

## 验证

- 单元测试覆盖 L1 命中、OneDrive 命中、过期覆盖、并发请求合并和上游失败回退。
- 浏览器验证搜索列表、底部播放器、全屏歌词页均出现真实封面。
- 对照 demo 检查首页、侧栏和搜索交互，列出仍因 API 缺失而不可用的功能。
- 执行 `pnpm format`、`pnpm lint`、`pnpm build` 和后端测试。
- 先本地构建验收，再按现有音乐站独立通道部署；不改 blog 的 FRP/Caddy 配置。
